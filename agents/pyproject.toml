[tool.poetry]
name = "demo-fetch-agent"
version = "0.1.0"
description = ""
authors = ["Jonathan Kopuri <kjernest01@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
